//include necessary library function
//including more to ensure I'm not missing, not cause I necessarily think I need
them
#include<stdio.h>
#include <stdlib.h>
#include <string.h>
#include "prj.h"
//just declare the remaining functions whose codes are not given
void terminateAndWrite(struct f1player_se* head); //Complete
void print_sameRacePoint(struct f1player_se* head); //complete
struct f1player_se* deleteApplication(struct f1player_se* head, struct f1player_se*
to_be_gone);//complete //Complete
void printFormatted(struct f1player_se* head); //complete
struct f1player_se* hiring(struct f1player_se* head, char* position); //complete
struct f1player_se* process(struct f1player_se* head, int* clockTime);//complete
void analyzeApplicantList(struct f1player_se* head); //complete
struct f1player_se* sortByWins(struct f1player_se* head); //complete
